import * as React from "react";
import { Button } from "./Button";

export default { title: "Button" };

export const button = () => (
  <div className="p-10">
    {" "}
    <Button>Sign Up</Button>
  </div>
);
